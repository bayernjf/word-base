{
  "buildCommand": "npm -w @wordbase/web run build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    { "source": "/app(/.*)?", "destination": "/app.html" },
    { "source": "/api(.*)", "destination": "/api/[...all].js" }
  ]
}
